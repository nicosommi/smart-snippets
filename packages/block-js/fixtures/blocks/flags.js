// archetype one-liner
// version 0.0.1
/* ph replacements */
/* name, /Apple/, Apple */
/* endph */
import debug from 'debug' // stamp debug (ignored)
const namespace = 'fruits.apple' // ph namespace

export default class Apple {
  eat () {
    /* stamp craf (ignored) */
    debug(namespace)('craf')
    /* endstamp */
  }
}
